# flask_auth_system
This repository contains a Flask authentication system built with JWT authentication. It is implemented using Python’s Flask framework and uses SQLite3 as the database for storing user data securely.
